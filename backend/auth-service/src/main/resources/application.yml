server:
  port: 8090

spring:
  datasource:
    url: ${DB_AUTH_URL}
    username: ${USERNAME}
    password: ${PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

jwt:
  secret-key: ${JWT_SECRET_KEY}
